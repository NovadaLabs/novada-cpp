#pragma once

namespace novada {

// kVersion is the semantic version of this SDK. It is embedded in the default
// User-Agent header sent on every request.
inline constexpr const char* kVersion = "0.1.0";

}  // namespace novada
